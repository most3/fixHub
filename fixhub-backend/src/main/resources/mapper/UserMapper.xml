<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fixhub.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.fixhub.user.model.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="passwordHash" column="password_hash" />
        <result property="displayName" column="display_name" />
        <result property="role" column="role" javaType="com.fixhub.user.model.enums.UserRole" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="selectById" parameterType="long" resultMap="UserResultMap">
        SELECT id, username, password_hash, display_name, role, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="selectByUsernameIgnoreCase" parameterType="string" resultMap="UserResultMap">
        SELECT id, username, password_hash, display_name, role, created_at
        FROM users
        WHERE LOWER(username) = LOWER(#{username})
    </select>

    <select id="existsByUsernameIgnoreCase" parameterType="string" resultType="boolean">
        SELECT CASE WHEN COUNT(1) > 0 THEN TRUE ELSE FALSE END
        FROM users
        WHERE LOWER(username) = LOWER(#{username})
    </select>

    <insert id="insert" parameterType="com.fixhub.user.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password_hash, display_name, role, created_at)
        VALUES (#{username}, #{passwordHash}, #{displayName}, #{role}, #{createdAt})
    </insert>

    <update id="update" parameterType="com.fixhub.user.model.User">
        UPDATE users
        SET username = #{username},
            password_hash = #{passwordHash},
            display_name = #{displayName},
            role = #{role}
        WHERE id = #{id}
    </update>

</mapper>
